import React from 'react';

import { Text } from 'react-native';

import { Container } from './styles';

const Logo: React.FC = () => {
  return (
    <Container>
      <Text>Logo</Text>
    </Container>
  );
};

export default Logo;
